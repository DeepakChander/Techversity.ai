import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Apply Now",
  description: "Start your journey to academic recognition. Apply for Honorary Doctorate, DBA, or PhD programs through Techversity.ai.",
};

export default function ApplyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
