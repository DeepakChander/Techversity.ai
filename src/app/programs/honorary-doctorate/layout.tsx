import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Honorary Doctorate",
  description: "Earn an Honorary Doctorate recognizing your distinguished professional achievements through Techversity.ai's accredited university partners.",
};

export default function HonoraryDoctorateLayout({ children }: { children: React.ReactNode }) {
  return children;
}
