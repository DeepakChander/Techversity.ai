import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Doctor of Business Administration (DBA)",
  description: "Pursue a Doctor of Business Administration (DBA) through Techversity.ai's accredited university partners for senior business leaders.",
};

export default function DBALayout({ children }: { children: React.ReactNode }) {
  return children;
}
