import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Doctor of Philosophy (PhD)",
  description: "Pursue a Doctor of Philosophy (PhD) through Techversity.ai's accredited university partners for research-driven professionals.",
};

export default function PhDLayout({ children }: { children: React.ReactNode }) {
  return children;
}
